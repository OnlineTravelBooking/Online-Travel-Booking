import React, { useState } from "react";
import {
  Button,
  Cascader,
  DatePicker,
  Form,
  Input,
  InputNumber,
  Mentions,
  Segmented,
  Select,
  TreeSelect,
  Layout,
} from "antd";

import { FilterOutlined } from "@ant-design/icons";

const { Sider } = Layout;
const { RangePicker } = DatePicker;
const { Option } = Select;

export default function SiderFilter() {
  return <div className="Filter-Box">James</div>;
}
